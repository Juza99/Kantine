# Host of the PartyKit server (no protocol, no trailing slash).
# Local development:
VITE_PARTYKIT_HOST=127.0.0.1:1999

# Production (after `npm run party:deploy`), something like:
# VITE_PARTYKIT_HOST=de-kantine.yourusername.partykit.dev
